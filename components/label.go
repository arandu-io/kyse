// Code generated by `aru view:build` from label.kyse.go. DO NOT EDIT.
//
// Edit the .kyse.go and run `aru view:build`. Changes here are overwritten.

package components

import (
	"html/template"
	"io"
	"strings"

	"github.com/arandu-io/framework/view"
)

//line components/label.kyse.go:5

// LabelProps is the text that names a control, tied to it by id.
//
// Field draws its own, so a labelled form row needs nothing from here. This is
// the other half of Input: when the caller lays the row out, the name is a
// separate element, and the two halves have to agree on one id.
//
// The required marker is drawn from a flag rather than written into Text,
// because it is decoration and has to be kept out of the accessible name: read
// aloud it turns "Email" into "Email star", and it is not what announces the
// constraint either -- the required attribute on the control is.
type LabelProps struct {
	// For is the id of the control this names, which is the control's id and not
	// its name. The two are the same only where nothing set them apart.
	For string
	// Text is what the label says.
	Text string
	// Required draws the marker beside the text. It is a mark on the page and
	// nothing more: the control carries the required attribute.
	Required bool
}

//line components/label.go:39

// Label renders the label component.
func Label(props LabelProps) template.HTML {
	d := props
	_ = d
	w := &strings.Builder{}
	var err error
	if err == nil {
		_, err = io.WriteString(w, "\n")
	}
	if err == nil {
		_, err = io.WriteString(w, "<label class=\"label\" for=\"")
	}
	if err == nil {
//line components/label.kyse.go:28
		_, err = io.WriteString(w, view.TextAttr(d.For))
//line components/label.go:56
	}
	if err == nil {
		_, err = io.WriteString(w, "\">\n")
	}
	if err == nil {
		_, err = io.WriteString(w, "\t")
	}
	if err == nil {
//line components/label.kyse.go:29
		_, err = io.WriteString(w, template.HTMLEscapeString(view.Text(d.Text)))
//line components/label.go:67
	}
	if err == nil {
		_, err = io.WriteString(w, "\n")
	}
//line components/label.kyse.go:30
	if d.Required {
//line components/label.go:74
		if err == nil {
			_, err = io.WriteString(w, "\t\t<span class=\"text-destructive\" aria-hidden=\"true\">*</span>\n")
		}
	}
	if err == nil {
		_, err = io.WriteString(w, "</label>\n")
	}
	if err == nil {
		_, err = io.WriteString(w, "\n")
	}
	_ = err
	return template.HTML(w.String())
}

var (
	_ = template.HTMLEscapeString
	_ = io.WriteString
	_ = view.Text
	_ = view.UnsafeText
	_ = strings.Builder{}
)
